import ChecklistConfig from "@/components/ChecklistConfig";

const ConfiguracoesChecklist = () => (
  <div className="max-w-6xl">
    <ChecklistConfig />
  </div>
);

export default ConfiguracoesChecklist;
